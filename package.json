{
  "name": "them1947",
  "private": true,
  "scripts": {
    "build": "node scripts/prepare-worker-deploy.js",
    "check": "node scripts/check-site.js",
    "check:dist": "node scripts/check-site.js --dist",
    "deploy": "npm run check && npm run build && npm run check:dist && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.123.0"
  }
}
